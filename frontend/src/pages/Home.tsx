import React from "react";
import "./Home.sass";
import VirtualKeyboard from "../components/VirtualKeyboard/VirtualKeyboard";

const Home = () => {
  return (
    <main>
      <VirtualKeyboard />
    </main>
  );
};

export default Home;
