import React from "react";
import { render, screen } from "@testing-library/react";
import Word_UI from "components/Word_UI/Word_UI";

describe("Word UI component test", () => {
  test("sample testing", () => {
    expect(true).toBe(true);
  });
});
